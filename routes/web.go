package routes

import (
	"github.com/gin-gonic/gin"
)

// Register used for register router.
func Register(engine *gin.Engine) {
	// 书籍
	//bookGroup := engine.Group("book")
	//{
	//controller := controllers.BookController{}
	//bookGroup.GET("GetBookList", controller.GetBookList) // 获取书籍列表
	//}
}
